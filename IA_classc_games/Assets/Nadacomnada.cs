using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Nadacomnada : MonoBehaviour
{
    // Start is called before the first frame update
    public int casada;
    void Start()
    {
        casada = 10;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
