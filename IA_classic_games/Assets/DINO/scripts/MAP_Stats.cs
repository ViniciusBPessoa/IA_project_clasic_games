using UnityEngine;

public class MAP_Stats : MonoBehaviour
{
    public int Map_velocidade;
    public int Map_pontuacao;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
